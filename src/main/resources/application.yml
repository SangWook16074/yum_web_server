spring:
  application:
    name: yum-ingredient-server

  config:
    import: optional:file:.env[.properties]

  sql:
    init:
      mode: always

  data:
    r2dbc:
      repositories:
        enabled: true

  r2dbc:
    url: r2dbc:pool:mysql://${DB_URL}?autoReconnect=true&useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}


springdoc:
  swagger-ui:
    use-root-path: true
  api-docs:
    version: openapi_3_1
    enabled: true
    path: /api/ingredients/v3/api-docs
  default-consumes-media-type: application/json
  default-produces-media-type: application/json
  enable-spring-security: true